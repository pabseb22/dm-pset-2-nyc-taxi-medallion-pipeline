# dm-pset-2-nyc-taxi-medallion-pipeline
Build a data pipeline orchestrated with Mage that ingests data from the NYC TLC Trip Record Data dataset, lands the bronze layer in PostgreSQL (Docker), standardizes and debugs in silver, and models a star schema in gold using dbt executed from Mage.
