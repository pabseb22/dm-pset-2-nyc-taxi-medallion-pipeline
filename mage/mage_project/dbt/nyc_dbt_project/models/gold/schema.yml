version: 2

models:
  - name: dim_date
    columns:
      - name: date_key
        tests:
          - not_null
          - unique

  - name: dim_zone
    columns:
      - name: zone_id
        tests:
          - not_null
          - unique

  - name: dim_service_type
    columns:
      - name: service_type_key
        tests:
          - not_null
          - unique

  - name: dim_payment_type
    columns:
      - name: payment_type_key
        tests:
          - not_null
          - unique
